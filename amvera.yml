meta:
  environment: python
  toolchain:
    name: pip
    version: "3.10"
build:
  requirementsPath: requirements.txt
  useCache: true

run:
  # Мы убираем scriptName и используем только явную команду
  command: python app.py
  persistenceMount: /data
  containerPort: 80